# Project TODO

- [pending] Implement the central auth authority and broker so session workers
  receive access tokens without mounting shared refresh-token state.
- [pending] Implement per-session storage snapshots, differential compression,
  retention, and cross-host restore verification independently of auth state.
